{% comment %}
  Renders the type of a parameter.
  [param] the parameter.
{% endcomment %}{% if param.enumType %}{{ param.parentPath|join:"" }}{{ param.enumType.className }}{% else %}{{ param.codeType }}{% endif %}